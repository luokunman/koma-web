# AI漫剧官网示例

这是一个基于 Next.js App Router + TypeScript 的 SSR 落地页示例项目，包含：

- 导航、Hero、平台预览、功能区、流程区、案例区、价格区、FAQ、CTA、Footer
- 客户端交互：滚动进度、导航滚动样式、返回顶部、FAQ 手风琴、移动菜单、深色模式、IntersectionObserver reveal、tilt
- 渐变 + 毛玻璃 + 响应式 + 深色模式视觉风格

## 运行方式

```bash
npm install
npm run dev
```

默认启动后访问：`http://localhost:3000`
